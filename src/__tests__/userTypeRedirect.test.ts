import request from 'supertest';
import { app } from '../app';

describe('UserTypeRedirect', () => {
  it('deve redirecionar admin para gerenciamento e paciente para agendamento', async () => {
    // ... implementação virá depois ...
  });
}); 