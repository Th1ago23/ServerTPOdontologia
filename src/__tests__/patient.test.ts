import request from 'supertest';
import { app } from '../app';

describe('PatientController', () => {
  it('deve criar, buscar, atualizar e deletar um paciente', async () => {
    // ... implementação virá depois ...
  });
}); 