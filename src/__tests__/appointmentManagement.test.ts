import request from 'supertest';
import { app } from '../app';

describe('AppointmentManagementController', () => {
  it('deve aprovar, rejeitar e listar agendamentos', async () => {
    // ... implementação virá depois ...
  });
}); 